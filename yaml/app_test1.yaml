✨ Attempting to apply the following manifest:

apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: example-application
spec:
  selector:
    matchLabels:
      app: example-application
  componentKinds:
    - group: core
      kind: Service
    - group: apps
      kind: StatefulSet
  descriptor:
    type: "example-application"
    version: "1.0.0"
    description: "Example Application"
    icons:
      - src: "https://example.com/example-application.txt"
    maintainers:
      - name: "John Doe"
        email: "johndoe@example.com"
    links:
      - description: "About this example application"
        url: "https://example.com"
    notes: "This is an example application."
  addOwnerRef: true

